# Build artifacts — regenerated by build.sh / evidence.sh; not checked in.
kernels/
obj/
bin/
interp/*.o
interp/dispatch_plain
interp/dispatch_unordered
*.o
# Keep results/ (the measured data, metadata, evidence CSVs) under version control.
results/*.log
